export const FEATURES_CONFIG = {
  // =========================================================
  // 1. ГЛОБАЛЬНІ РЕЖИМИ (GLOBAL MODES)
  // Використовуйте для акцій ("безкоштовний тиждень") або 
  // швидкого перетворення проекту на повністю безкоштовний/платний.
  // =========================================================

  // Якщо true, УСІ користувачі отримують доступ до Premium (ліміти знімаються).
  UNLOCK_ALL_PREMIUM_FEATURES: false,
  
  // Якщо true, УСІ користувачі додатково отримують максимальний доступ (Realtor).
  UNLOCK_ALL_REALTOR_FEATURES: false,

  // =========================================================
  // 2. НАЛАШТУВАННЯ UI
  // =========================================================

  // Якщо false, сторінка підписок (/subscription) буде вимкнена, 
  // а всі посилання на неї (в хедері, фільтрах) будуть сховані.
  ENABLE_SUBSCRIPTIONS_PAGE: false,

  // =========================================================
  // 3. ОБМЕЖЕННЯ ДЛЯ БЕЗКОШТОВНИХ КОРИСТУВАЧІВ (FREE TIER LIMITS)
  // Діють тільки тоді, коли UNLOCK_ALL_PREMIUM_FEATURES = false
  // =========================================================

  // Категорії, доступні безкоштовним користувачам. Інші категорії сховані.
  // Доступні категорії: 
  // 'economics' (Економіка), 'education' (Освіта), 'medicine' (Медицина),
  // 'commerce' (Комерція), 'culture_leisure' (Культура/Відпочинок), 
  // 'sports' (Спорт), 'transport' (Транспорт), 'security' (Безпека), 'utilities' (Комуналка)
  FREE_ALLOWED_CATEGORIES: ['medicine', 'transport', 'commerce'],

  // Ліміт кількості районів для відображення без підписки.
  FREE_DISTRICTS_LIMIT: 5,
  
  // Максимальна кількість фільтрів для безкоштовних користувачів.
  FREE_FILTERS_LIMIT: 2,

  // =========================================================
  // 4. ПЕРЕВИЗНАЧЕННЯ ПОЛІВ БАЗИ ДАНИХ (OVERRIDES)
  // Ручне перевизначення доступу до конкретних полів (перекриває БД).
  // =========================================================
  
  // Формат: 'dbKey_поля': { isPremiumField: true/false, isRealtorOnly: true/false }
  FIELD_OVERRIDES: {} as Record<string, { isPremiumField?: boolean; isRealtorOnly?: boolean }>,
};

